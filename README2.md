1. Firebase Console에서 서비스 계정 키 발급:
- https://console.firebase.google.com/ → 프로젝트 선택
- ⚙️ 설정 → 서비스 계정 → 새 비공개 키 생성
- JSON 파일 다운로드
2. 다운로드한 파일을 다음 위치에 저장:
- src/main/resources/firebase-service-account.json